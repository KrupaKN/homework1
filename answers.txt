What is the difference between a merge and a rebase?

Merge integrates changes between branches while preserving the commit history of the branches while rebase rewrites the commit history. Merge combines the changes between two branches while rebase moves the changes between branches.

